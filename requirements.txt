# Core dependencies for RAG PoC deployment
streamlit>=1.40.0
pinecone-client>=3.0.0
sentence-transformers>=2.2.0
requests>=2.31.0

# PyTorch CPU-only (lightweight for deployment)
--extra-index-url https://download.pytorch.org/whl/cpu
torch
